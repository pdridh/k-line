version: "2"

sql:
    - engine: "postgresql"
      queries: "./db/queries"
      schema: "./db/migration"
      gen:
          go:
              package: "sqlc"
              out: "./db/sqlc"
              emit_interface: true
              emit_db_tags: true
              sql_package: "pgx/v5"
